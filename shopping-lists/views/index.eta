<% layout("./layouts/layout.eta") %>

<h1>Shared shopping list</h1>

<% if ( it.shoppingListsCount ) { %>
	<ul>
		<li>Shopping lists: <%= it.shoppingListsCount %></li>
		<li>Shopping list items: <%= it.shoppingItemsCount %></li>
	</ul>
<% } else { %> 
	<p>No shopping lists yet.</p>
<% } %>


<h2>Here will be stats...</h2>

<a href="/lists">Lists</a>
